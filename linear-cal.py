def cal():
    y = int(input('Enter a valid number for "y": '))
    c = int(input('Enter a valid number for "c": '))
    m =int(input('Enter a valid number for "m": '))
    x = (y - c) / m
    
    

    print(f"\n X is = {x}")
cal()
    
